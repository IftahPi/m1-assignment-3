# --- Secrets (NEVER commit) ---
.env

# --- Python virtual environment ---
.venv/

# --- Python bytecode / caches ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- Runtime-generated agent state ---
*.sqlite
*.sqlite3
checkpoints.sqlite
profiles/

# --- LangGraph Studio runtime cache (created by `langgraph dev`) ---
.langgraph_api/

# --- memory_eval.py isolated state (separate from the main checkpointer/profile) ---
checkpoints_memory_eval.sqlite
profiles_memory_eval/

# --- Cached dataset (re-downloaded by dataset/loader.py on first run) ---
data/

# --- IDE / OS noise ---
.idea/
.DS_Store

# --- Personal working notes (not part of the submission) ---
wip.txt

# --- Course materials (private notes only; never submitted) ---
classroom/
